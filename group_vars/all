# True if python is needed to be installed first.
install_python: False

# Distro of python which has to be installed.
python_package: python

ansible_python_interpreter: /usr/bin/python

# Exported directory on nfs server.
exported_dir: /var/share

# Mountable directories on nfs clients.
mountable_dir: /mnt/nfs/share
